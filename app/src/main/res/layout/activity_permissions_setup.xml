<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1e293b"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="@dimen/spacing_large"
        android:paddingEnd="@dimen/spacing_large"
        android:paddingTop="@dimen/spacing_large"
        android:paddingBottom="0dp">

        <!-- Header - Modern Shield Icon with Small Circular Background -->
        <androidx.cardview.widget.CardView
            android:id="@+id/shieldIconCard"
            android:layout_width="110dp"
            android:layout_height="110dp"
            android:layout_gravity="center"
            android:layout_marginTop="@dimen/spacing_xlarge"
            app:cardBackgroundColor="#2c5282"
            app:cardCornerRadius="55dp"
            app:cardElevation="8dp">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="18dp"
                android:src="@drawable/ic_permission_shield"
                android:contentDescription="Security Shield" />
        </androidx.cardview.widget.CardView>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="@dimen/spacing_large"
            android:text="App Permissions"
            android:textColor="@color/text_primary"
            android:textSize="28sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="@dimen/spacing_small"
            android:gravity="center"
            android:text="To provide you with the best experience,\nwe need access to the following permissions:"
            android:textColor="@color/text_secondary"
            android:textSize="14sp" />

        <!-- Permissions List -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_large"
            android:orientation="vertical">

            <!-- Notifications -->
            <include layout="@layout/item_permission"
                android:id="@+id/permissionNotifications" />

            <!-- Camera -->
            <include layout="@layout/item_permission"
                android:id="@+id/permissionCamera" />

            <!-- Photos and Videos -->
            <include layout="@layout/item_permission"
                android:id="@+id/permissionPhotos" />

            <!-- SMS -->
            <include layout="@layout/item_permission"
                android:id="@+id/permissionSms" />


        </LinearLayout>

        <!-- Buttons -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnAllowAll"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="@dimen/spacing_large"
            android:text="Allow All Permissions"
            android:textAllCaps="false"
            android:textColor="#0f172a"
            android:textSize="16sp"
            android:textStyle="bold"
            app:backgroundTint="@color/white"
            app:cornerRadius="12dp"
            app:elevation="0dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSkip"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_small"
            android:layout_marginBottom="@dimen/spacing_medium"
            android:text="Skip for now"
            android:textAllCaps="false"
            android:textColor="#94a3b8"
            android:textSize="16sp" />

    </LinearLayout>

</ScrollView>
